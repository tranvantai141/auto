import { sdkManagersHelper } from './sdk-managers-helper';

describe('sdkManagersHelper', () => {
  it('should work', () => {
    expect(sdkManagersHelper()).toEqual('sdk-managers-helper');
  });
});
